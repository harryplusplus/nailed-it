# Project Guidelines

## TypeScript

After making changes to TypeScript files, always run:

```bash
pnpm oxfmt    # Format code
pnpm oxlint   # Lint (includes type checking)
```

Fix any errors or warnings before committing.